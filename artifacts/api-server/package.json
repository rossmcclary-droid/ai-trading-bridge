{
  "name": "@workspace/api-server",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8080} --reload",
    "build": "python -m compileall -q app",
    "start": "uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8080}",
    "typecheck": "python -m compileall -q app"
  }
}
